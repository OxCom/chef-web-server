sudo: required
before_script:
  # get chef installer
  - wget https://www.opscode.com/chef/install.sh -O chef.sh
  # install chef
  - sudo bash chef.sh
  # get version
  - chef-solo -v
rvm:
  - 1.9.3

install:
  - sudo bash chef.sh